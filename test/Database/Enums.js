// Enum using integer values
export const IntegerStatus = {
  draft: 0,
  pending: 1,
  done: 2
}

// Enum without explicit integer values
export const NonBackedStatus = {
  draft: 'draft',
  pending: 'pending',
  done: 'done'
}
